# Phase 5 — Retrieval

## Summary
Takes Phase 4's query variants (raw query + optional HyDE doc + multi-query paraphrases + decomposed sub-questions) and turns them into a final ranked candidate set: dense ANN search + sparse BM25 search per variant, RRF fusion across all of them, then BGE cross-encoder reranking against the original raw query. `RetrievalPipeline.retrieve()` is the single entry point Phase 6 calls. No state left on disk — reads the Phase 3 Qdrant collection, returns an in-memory list.

## Worked Example
Continuing from Phase 4: query *"What was Apple's net sales in fiscal 2023?"*, with `TransformedQuery.hyde_doc` + 3 `multi_queries` from that phase.

1. `build_query_variants()` collects 5 distinct strings to search with: the raw query, the HyDE passage, and the 3 multi-query paraphrases (deduped — none happened to collide here).
2. `HybridRetriever.retrieve(variants)` runs dense ANN search **and** sparse BM25 search against the `financial_rag` collection (1,177 points: 737 AAPL + 440 MSFT chunks) for *each* of the 5 variants — 10 searches total — then RRF-fuses all 10 ranked lists into one.
3. `Reranker.rerank(raw_query, fused)` re-scores the fused candidates with the BGE cross-encoder against the *original* raw query (not the HyDE doc or paraphrases), truncates to `top_k_rerank=5`. Real output for this query:

   | Rank | Score | Sources | Item | Chunk preview |
   |---|---|---|---|---|
   | [1] | 0.9964 | dense+sparse | Item 8 | "...The U.S. and China were the only countries that accounted for more than 10% of the Company's net sales in 2023..." |
   | [2] | 0.9833 | dense+sparse | Item 8 | "...Net sales disaggregated by significant products and services for 2023, 2022 and 2021..." |
   | [3] | 0.9813 | dense only | Table 19 | "iPhone (1) (2023): $. iPhone (1) (): 200,583. Mac (1) (2023): 29,357..." (NL-sentence table chunk) |
   | [4] | 0.9796 | dense+sparse | Item 7 | "...(2) Services net sales include amortization... iPhone net sales..." |
   | [5] | 0.9779 | dense only | Item 7 | "...Products and Services Performance The following table shows net sales by category..." |

   Notice the exact chunk from Phases 2-3's worked example (`33c5dee5...`, the "Fiscal Year Highlights" paragraph) **did not** make this top-5 — a different Item 7/Item 8/Table 19 set of chunks won instead, all of which also state the $383,285M figure (just via different sentences/tables). This matters later: Phase 7's `judge_relevance()` heuristic checks for textual overlap against one *specific* gold passage, so if the gold evidence happens to be the "Fiscal Year Highlights" sentence specifically, these 5 retrieved-but-differently-worded chunks would score as "not relevant" by that heuristic even though they answer the question correctly — see Phase 7's Worked Example for this exact scenario playing out.
4. Also notice `[1]` and `[2]` were found by both dense *and* sparse search (`sources=["dense", "sparse"]`) — both signals agreed, which is reflected in RRF giving them the top fused ranks before reranking even runs. `[3]` and `[5]` were dense-only: the cross-encoder still ranked them highly because they're genuinely on-topic, even though sparse/BM25 missed them (likely because the table-derived NL-sentence chunks use different surface wording than the query).

This 5-chunk list is exactly what Phase 6 receives next.

## Files Changed / Added
- `src/retrieval/models.py` — new — `RetrievedChunk` (chunk + score + sources), the pydantic boundary into Phase 6
- `src/retrieval/dense_retriever.py` — modified (stub → full) — `DenseRetriever.search()`: embeds query via `BGEEmbedder`, ANN search against the `dense` named vector
- `src/retrieval/sparse_retriever.py` — modified (stub → full) — `SparseRetriever.search()`: encodes query via the persisted BM25 vocab, search against the `sparse` named vector
- `src/retrieval/hybrid_retriever.py` — modified (stub → full) — `rrf_fuse()` (Reciprocal Rank Fusion across N ranked lists) + `HybridRetriever.retrieve()` (runs dense+sparse over every query variant, fuses)
- `src/retrieval/reranker.py` — modified (stub → full) — `Reranker.rerank()`: BGE cross-encoder re-scores candidates against the original query, truncates to `top_k_rerank`
- `src/retrieval/retrieval_pipeline.py` — new — `build_query_variants()` + `RetrievalPipeline.retrieve()`: wires the whole phase into one call
- `src/indexing/sparse_indexer.py` — modified — minor fix to keep BM25 encoding consistent between index-time and query-time
- `scripts/retrieve.py` — new — CLI smoke-test: prints dense/sparse/hybrid/reranked stages side by side for one query
- `tests/unit/test_dense_retriever.py`, `test_sparse_retriever.py`, `test_hybrid_retriever.py`, `test_reranker.py`, `test_retrieval_pipeline.py` — new

## Key Design Decisions
- **Query variants are passed into `RetrievalPipeline`, not generated by it**: Phase 4 already owns HyDE/multi-query/decomposition and each needs an LLM call — keeping retrieval LLM-free means it stays testable without an Ollama client, same separation `IndexManager` keeps from chunking.
- **`RetrievedChunk.score` is a single float, not per-source**: dense cosine similarity and BM25 scores are on incomparable scales — only rank within each source's list matters, which is what RRF consumes. The field holds whichever stage most recently set it (raw score → fused RRF score → cross-encoder score), so callers never need to know which stage produced "the current ranking score."
- **RRF (parameter-free) over weighted-sum fusion**: no tunable weight to overfit to a small eval set, and it's the standard hybrid-search baseline.
- **Reranking always scores against the raw query, never a HyDE doc or paraphrase**: those exist only to widen retrieval recall — the user's actual question is what should determine final relevance ranking.
- **`build_query_variants()` dedupes while preserving order**: `DecomposedQuery.sub_questions` is `[raw_query]` when decomposition is off, so without dedup the raw query would double-count in RRF fusion.

## Execution Flow
1. `scripts/retrieve.py --query "..."` invoked
2. `load_config()` resolves `configs/base.yaml`
3. `RetrievalPipeline(cfg)` opens the embedded `QdrantClient`, builds `HybridRetriever` + `Reranker`
4. `retrieve(raw_query, transformed, decomposed)` → `build_query_variants()` collects every distinct query string
5. `HybridRetriever.retrieve(variants)` → dense + sparse search per variant → `rrf_fuse()`
6. `Reranker.rerank(raw_query, fused)` → cross-encoder re-scores, truncates to `top_k_rerank`
7. CLI prints each stage (dense/sparse/hybrid/reranked) as a rich table plus a dense-vs-sparse overlap summary

## Data Contract (Input → Output)
- Input: raw query `str` + optional `TransformedQuery` / `DecomposedQuery` (Phase 4)
- Output (consumed by Phase 6): `list[RetrievedChunk]`, length ≤ `top_k_rerank`, cross-encoder-scored, best-first

## Tests
- `test_dense_retriever.py` / `test_sparse_retriever.py` — search against an in-memory Qdrant collection, fake embedder/encoder
- `test_hybrid_retriever.py` — RRF fusion math, one dense+sparse call per query variant
- `test_reranker.py` — sort-by-score + truncation, fake `CrossEncoder`
- `test_retrieval_pipeline.py` — `build_query_variants()` dedup/ordering, pipeline wiring with fake retriever/reranker
- Full suite: 93 passed (`python -m pytest tests/unit -q`)
- Verified against the real AAPL 10-K 2023 collection: dense, sparse, and reranked stages all return relevant hits for a real query

## Config Keys Used
- `configs/base.yaml` → `retrieval.top_k_dense`, `retrieval.top_k_sparse`, `retrieval.top_k_rerank`, `retrieval.reranker`, `indexing.collection_name`, `indexing.qdrant_path`, `embeddings.dense_model`, `embeddings.batch_size`

## Open Items / Deferred
- `hybrid_alpha` config key exists but isn't used — RRF fusion is parameter-free by design; the key is a leftover from an earlier weighted-sum design considered and dropped.
- No script wires Phase 4's `QueryAnalyzer`/`QueryTransformer`/`QueryDecomposer` into `scripts/retrieve.py` — it only searches the raw query. Full Phase 1-6 wiring happens in `scripts/generate.py` (Phase 6).
